import {
    pushUser,
    removeUser,
    registerUser,
    loginUser,
    logoutUser,
    refreshToken,
} from 'behavior/authentication';

const actions = {
    pushUser,
    removeUser,
    registerUser,
    loginUser,
    logoutUser,
    refreshToken,
};

export default actions;
